[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "odnn"
version = "0.1.0"
description = "Optical diffractive neural network utilities"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["odnn*"]
